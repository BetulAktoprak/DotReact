export interface IProduct
{
    name: string
    description: string
    price: number
    isActive: boolean
    imageUrl: string
    stock: number
    id: string
    createdDate: string
    updatedDate: string
}